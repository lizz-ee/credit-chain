CREDIT-CHAIN PART 4 (RETRY)
Drop these files into your repo (overwrite):
- src/App.jsx
- src/main.jsx

This zip only contains wiring (provider wrap + navigation glue).
If your project uses a different ProposalDetailScreen/OffWorld screen for non-sim, keep yours.
